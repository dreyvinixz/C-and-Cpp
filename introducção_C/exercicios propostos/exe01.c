#include <stdio.h>

int main(){
    /* code */
    int r;
    float pi, a;
    pi = 3.14;
    r = 2;
    a = pi * (r * r);
    printf("a area do circuferia de raio 2 e: %.2f\n", a); 

    /* code */
    int c = 100;
    float f = (9 * c + 160) / 5;
    
    printf("%.2f\n", f);

    return 0;
}
